/** users.ts — TODO: port from users.html */

export function init(el: HTMLElement): void {
  el.innerHTML = `
    <div class="page-header">
      <span class="page-title">Users</span>
    </div>
    <div class="card">
      <p style="color:var(--muted)">View coming soon — port from the GAS HTML.</p>
    </div>
  `;
}
