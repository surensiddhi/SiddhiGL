/** posted-list.ts — TODO: port from posted-list.html */

export function init(el: HTMLElement): void {
  el.innerHTML = `
    <div class="page-header">
      <span class="page-title">Posted List</span>
    </div>
    <div class="card">
      <p style="color:var(--muted)">View coming soon — port from the GAS HTML.</p>
    </div>
  `;
}
