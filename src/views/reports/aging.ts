/** aging.ts — TODO: port from Rpt_*.html */

export function init(el: HTMLElement): void {
  el.innerHTML = `
    <div class="page-header">
      <span class="page-title">Aging</span>
    </div>
    <div class="card">
      <p style="color:var(--muted)">Report coming soon — port from the GAS HTML.</p>
    </div>
  `;
}
