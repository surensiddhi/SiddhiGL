/** company-config.ts — TODO: port from company-config.html */

export function init(el: HTMLElement): void {
  el.innerHTML = `
    <div class="page-header">
      <span class="page-title">Company Config</span>
    </div>
    <div class="card">
      <p style="color:var(--muted)">View coming soon — port from the GAS HTML.</p>
    </div>
  `;
}
